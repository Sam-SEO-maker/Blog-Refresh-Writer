# Copy to `.env` and fill in. Git-ignored: never shared, never committed.
# Guide: onboarding/01-setup-machine.md (step 7). Values: maintainer's welcome kit.

# Shared — from the welcome kit
DATAFORSEO_LOGIN=
DATAFORSEO_PASSWORD=
YTG_NEW_API_KEY=

# GSC: nothing to fill (shared MCP server, auth is server-side).

# Your blog's WordPress — from the tech team, in step 3b of onboarding 02.
# Use the exact names you wrote in your site.json (example: superprof.it):
# WP_SUPERPROF_IT_USER=
# WP_SUPERPROF_IT_APP_PASSWORD=

# Your Google Sheet — only if you use /batch or /decide (onboarding 03).
# SPREADSHEET_ID_<YOUR_SITE>=
